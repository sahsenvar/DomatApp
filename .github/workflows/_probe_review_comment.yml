name: probe-review-comment
on:
  pull_request_review_comment:
    types: [created]
  pull_request_review:
    types: [submitted, edited]
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo "PROBE fired event=${{ github.event_name }} pr=${{ github.event.pull_request.number }}"
